export default interface SliderParams {
	dots?: boolean,
	arrows?: boolean,
	freeMode?: boolean,
	slidesToShow?: number,
	slidesToScroll?: number,
	marginBetweenSlides?: number,
	arrowSize?: string,
	dotsPos?: string,
	timer?: number
}